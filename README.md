# blocks.py
usage: blocks.py [-h] [-a] [-v] [-t] [-s SHAPES_TYPE] [-l LEVEL] [-m]

ch

options:
  -h, --help            show this help message and exit
  -a, --automate        automate
  -v, --verbose         verbose
  -t, --test            test
  -s SHAPES_TYPE, --shapes_type SHAPES_TYPE
                        shapes_types. n:normal, a:additional,r:random
  -l LEVEL, --level LEVEL
                        level. 1 - 10
  -m, --mode            mode:false=step,true=continous